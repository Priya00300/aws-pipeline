{
  "name": "hello-world-pipeline",
  "version": "1.0.0",
  "description": "Hello World app for AWS pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Tests passed successfully!\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "18.x"
  }
}